import {
    createStyles,
  } from '@mantine/core';
export const useContentStyle = createStyles(() => ({
  body:{
    height:"100vh",
    maxWidth:"100%",
    padding:"0",
    margin:"0",
    // backgroundImage: 'url("background.png")',
    // // backgroundRepeat: 'no-repeat',
    // backgroundSize: 'cover'
  }
  
  }));